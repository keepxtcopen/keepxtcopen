{
  "name": "keepxtcopen",
  "private": true,
  "type": "module",
  "scripts": {
    "docs:dev": "node scripts/generate-news.js && vitepress dev docs",
    "docs:build": "node scripts/generate-news.js && vitepress build docs && node scripts/generate-rss.js",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
